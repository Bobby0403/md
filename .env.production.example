# 生产环境配置示例
# 复制此文件为 .env.production 并根据实际情况修改配置

# ============================================
# iframe 安全配置（重要！）
# ============================================

# 生产环境 iframe 允许的域名白名单
# 生产环境默认不允许任何域名，必须显式配置此变量
# 
# 配置说明：
# 1. 只允许 HTTPS 域名（安全要求）
# 2. 使用精确匹配或通配符匹配
# 3. 多个域名用逗号分隔
#
# 示例配置：
# - 单个域名：["https://example.com"]
# - 多个域名：["https://example.com", "https://app.example.com"]
# - 通配符：["https://*.example.com"] 允许所有 example.com 的子域名
# - 混合配置：["https://example.com", "https://*.example.org"]
#
# ⚠️ 警告：生产环境必须配置此变量，否则编辑器将拒绝所有 iframe 通信！
VITE_IFRAME_ALLOWED_ORIGINS=["https://example.com", "https://*.example.com"]

# ============================================
# 其他配置
# ============================================

# VSCode 调试器启动编辑器（可选）
# VITE_LAUNCH_EDITOR=code
